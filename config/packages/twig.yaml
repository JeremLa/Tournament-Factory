twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'bootstrap_4_layout.html.twig'
    globals:
        version: 0.2.3
        appTitle: 'Tournament Factory'
        tournamentRule: "@rules.tournament"
